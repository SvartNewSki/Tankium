1) при переходе return view('/') переходит на /buy,
 и в каком-то случае выдаёт undefined $products из @foreach $products
2)Если товар А купили другой , а в корзину я уже успел его положить, при проверке должна отбиваться ошибка, но return redirect back with
 Не работает правильно, начинает какой-то тягомотный редирект, который в итоге уходит в ошибку  
 ======
3) Если добавить товар в корзину из страницы товара и нажать назад 
 он возвращает на ту же страницу товара (вероятнее всего виноват редирект back)